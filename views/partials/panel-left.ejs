<div class="col-sm-12 col-md-12 col-lg-3">
    <img src="/images/myawesomeme.png" alt="My Awesome Meme Logo" class="img-fluid">
        <!-- Profile picture card-->
        <div class="card mb-4 mb-xl-0">
            <div class="card-header">Profile</div>
            <div class="card-body text-center">
                <!-- Profile picture image-->

                <img class="img-account-profile rounded-circle mb-2" src="<%= gravatar %>" alt="Gravatar">
                <!-- Profile picture help block-->
                <div class="small font-italic text-muted mb-4">
                    <% if(!currentUser) { %>
                        <a class="btn btn-primary" href="/login">Login</a>
                        <br />
                        <small>Don't have an account? <a href="/login">Register</a></small>
                    <% } else { %>
                        Welcome, <%=currentUser.username.toUpperCase() %><br />
                        <a class="btn btn-primary" href="/logout">Logout</a>
                    <% } %>
                </div>
                <!-- Profile picture upload button-->
            </div>
        </div>
</div>
